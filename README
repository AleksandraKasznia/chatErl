Compile and usage manual:
make to compile
make clean to decompile
erl to start the Erlang shell
mainServer:start(port) to start the server on port
mainServer:stop() or ctrl+c and then a to stop

Client Connection Protocol:

LOGIN username = Login
LOGOUT= Logout
MSG username message = Message a user
JOIN #room = Joins existing or creates, then joins a chatroom
PART #room= Leaves a joined room, removes room if this was last user in room
MSG #room message = Messages all users in a room
BROADCAST message =send braoadcast message
PBRO = unscubscribe broadcast
The server will respond with either an OK\r\n or ERROR reason\r\n
